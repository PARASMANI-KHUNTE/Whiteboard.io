# MongoDB Connection URI (optional - defaults to automatic local JSON file persistence if empty or unreachable)
MONGODB_URI=

# Google OAuth Credentials (optional - for Google Sign-In button)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Public application base URL for OAuth redirect URIs (e.g. http://localhost:3000)
APP_URL=
